// EXCHANGE WALLETS
// Etherscan is not cooperative and does not want to give more than 7 rows of data for some reason
// Go there https://etherscan.io/accounts/label/exchange?size=1000&start=0&col=1&order=asc
// Open console and run the following
// JSON.stringify([...$('td a')].map(a => a.textContent.toLowerCase()))
// Copy paste into exchangeWallets.json
// Profit

// CONTRACTS
// Go there https://etherscan.io/accounts/label/token-contract?subcatid=0&size=3200&start=0&col=1&order=asc
// Open console and run the following
// JSON.stringify([...$('td a')].map(a => a.textContent.toLowerCase()))
// Copy paste into contracts.json
// Profit
